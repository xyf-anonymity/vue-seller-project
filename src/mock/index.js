/* import Mock from 'mockjs'
import data from './data'
const BASEURL = 'http://localhost:8000'
Mock.mock(`${BASEURL}/api/seller`, {
    status: 0,
    data:data.seller,
})
Mock.mock(`${BASEURL}/api/goods`, {
    status: 0,
    data:data.goods,
})
Mock.mock(`${BASEURL}/api/ratings`, {
    status: 0,
    data:data.ratings,
}) */